* maxcut_qaoa_zne is the experiment notebook.

* Each problem has its own folder.

* Each problem folder has:
	- a folder that has the json for noiseless execution
	- a folder that has the json for noisy execution
	- a folder that has the jsons for zne executions
	- a results notebook, where we will make different data analysis
	- a imgs folder for saving different results